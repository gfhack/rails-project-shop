<!DOCTYPE html>
<html>
  <head>
    <title>
      Pós
    </title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <%= link_to t('home'), root_path, class: 'navbar-brand' %>
      <%= link_to t('products'), products_path, class: 'navbar-brand' %>
      <div class="collapse navbar-collapse">
        <div class="navbar-nav">
          <% if user_signed_in? %>
            <%= link_to('Logout', destroy_user_session_path, method: :delete, class: 'nav-item nav-link') %>
          <% end %>
        </div>
      </div>
    </nav>

    <main class="container mt-2" role="main">
      <%= render 'flash' %>

      <%= yield %>
    </main>
  </body>
</html>
