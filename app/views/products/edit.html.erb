<h1>
  <%= t('editing', name: t('product.name', count: 1)) %>
</h1>

<%= render 'form' %>

<br />

<%= link_to t('show'), @product, class: 'btn btn-info', role: :button %>
<%= link_to t('index'), products_path, class: 'btn btn-outline-secondary', role: :button %>
